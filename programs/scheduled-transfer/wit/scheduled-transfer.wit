package rialo:scheduled-transfer@0.1.0;

use rialo:primitives@0.1.0.{pubkey, lamports, timestamp, error};

world scheduled-transfer {
    export get-state: func() -> result<_, error>;
    export schedule: func(recipient: pubkey, amount-kelvin: u64, execute-at: u64) -> result<_, error>;
}
